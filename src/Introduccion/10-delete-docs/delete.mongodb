use("genesis_store");

// Elimina el documento con _id igual a 1
db.products.deleteOne({ _id: 1 });

// Elimina todos los documentos con price igual a 100
db.products.deleteMany({ price: 100 });

// Elimina todos los documentos
db.products.drop()